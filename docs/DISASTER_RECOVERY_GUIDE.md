# Disaster recovery guide

Declare the incident, stop unsafe writes, preserve evidence and identify the last verified backup and compatible application image. Restore into an isolated database, apply integrity and Alembic checks, verify learner ownership, authentication, audit events and object metadata, then rehearse cutover and rollback. Human approval is required before production cutover. Record recovery point and recovery time evidence.
