# Pronunciation Assessment

Scores use a validated 0–100 range. The bundled deterministic provider is explicitly labelled `SYNTHETIC_NOT_ACOUSTIC`; it is a workflow demonstration, not a claim of real acoustic accuracy. A real deployment must inject an acoustic provider through the same contract.
