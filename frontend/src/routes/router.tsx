import{createContext,useCallback,useContext,useEffect,useMemo,useState,type ReactNode}from"react";import type{AppRoute}from"../models";
const publicRoutes=new Set<AppRoute>(["/","/pricing","/privacy","/terms","/refunds","/support","/faq","/login","/register"]);const allRoutes=new Set<AppRoute>([...publicRoutes,"/onboarding","/app/dashboard","/app/daily-lesson","/app/conversation","/app/progress","/app/settings","/app/feedback","/app/subscription","/app/founder"]);interface RouterValue{route:AppRoute;navigate:(route:AppRoute,replace?:boolean)=>void;isPublic:boolean}const Context=createContext<RouterValue|null>(null);function current():AppRoute{const path=window.location.pathname as AppRoute;return allRoutes.has(path)?path:"/"}
export function RouterProvider({children}:{children:ReactNode}){const[route,setRoute]=useState(current);useEffect(()=>{const listener=()=>setRoute(current());window.addEventListener("popstate",listener);return()=>window.removeEventListener("popstate",listener)},[]);const navigate=useCallback((next:AppRoute,replace=false)=>{history[replace?"replaceState":"pushState"]({},"",next);setRoute(next)},[]);const value=useMemo(()=>({route,navigate,isPublic:publicRoutes.has(route)}),[route,navigate]);return <Context.Provider value={value}>{children}</Context.Provider>}export function useRouter(){const value=useContext(Context);if(!value)throw new Error("RouterProvider missing");return value}
