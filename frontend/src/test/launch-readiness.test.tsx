import{render,screen}from"@testing-library/react";import{describe,expect,it}from"vitest";import{App}from"../App";import{AuthProvider}from"../auth/AuthProvider";import{RouterProvider}from"../routes/router";
function renderApp(){return render(<RouterProvider><AuthProvider><App/></AuthProvider></RouterProvider>)}
describe("launch readiness",()=>{it("renders the closed-beta landing page",()=>{history.replaceState({},"","/");renderApp();expect(screen.getByRole("heading",{name:/Speak English with confidence/})).toBeVisible();expect(screen.getByText(/No public release yet/)).toBeVisible()});it("renders pricing without a live-payment claim",()=>{history.replaceState({},"","/pricing");renderApp();expect(screen.getByText("₹299")).toBeVisible();expect(screen.getByText(/test mode only/)).toBeVisible()})});
