import{render,screen,waitFor}from"@testing-library/react";import userEvent from"@testing-library/user-event";import{beforeEach,describe,expect,it,vi}from"vitest";import{api}from"../api/client";import{RouterProvider}from"../routes/router";import{DashboardScreen,ConversationScreen}from"../screens/ExperienceScreens";import{TutorPicker}from"../screens/TutorPicker";import{account,ananya,arjun,dashboard,preference}from"./fixtures";
beforeEach(()=>{vi.spyOn(HTMLMediaElement.prototype,"play").mockResolvedValue(undefined);vi.spyOn(HTMLMediaElement.prototype,"pause").mockImplementation(()=>undefined);vi.spyOn(HTMLMediaElement.prototype,"load").mockImplementation(()=>undefined);Object.defineProperty(URL,"createObjectURL",{configurable:true,value:vi.fn().mockReturnValue("blob:browser-acceptance")});Object.defineProperty(URL,"revokeObjectURL",{configurable:true,value:vi.fn()});vi.spyOn(api,"tutors").mockResolvedValue([ananya,arjun]);vi.spyOn(api,"savePreference").mockResolvedValue(preference);vi.spyOn(api,"conversation").mockResolvedValue({id:"conversation-1"});vi.spyOn(api,"turn").mockResolvedValue({turn_id:"browser-acceptance-turn",tutor_message:"Thanks for sharing.",next_question:"What happened next?",incorrect_span:"go",corrected_form:"went",correction_explanation:"Use the past tense here.",vocabulary_suggestions:["confident","routine"],telugu_explanation:"ఇక్కడ భూతకాలం ఉపయోగించండి."});vi.spyOn(api,"speech").mockResolvedValue({blob:new Blob([new Uint8Array(64)],{type:"audio/mpeg"}),provider:"openai",model:"gpt-4o-mini-tts",voice:"marin",cacheStatus:"MISS",inputCharacters:42,providerRequests:1,usageClassification:"provider-token-usage-unavailable"})});
describe("deterministic browser acceptance",()=>{for(const [name,width] of [["mobile",390],["tablet",768],["desktop",1440]] as const)it(`renders ${name} viewport`,()=>{Object.defineProperty(window,"innerWidth",{configurable:true,value:width});const{unmount}=render(<RouterProvider><DashboardScreen data={dashboard} tutor={ananya}/></RouterProvider>);expect(screen.getByText("Ready for today’s conversation?")).toBeVisible();unmount()});it("selects female and male tutors accessibly",async()=>{render(<RouterProvider><TutorPicker/></RouterProvider>);const female=await screen.findByRole("radio",{name:/Ananya/});const male=screen.getByRole("radio",{name:/Arjun/});await userEvent.click(female);expect(female).toHaveAttribute("aria-checked","true");await userEvent.click(male);expect(male).toHaveAttribute("aria-checked","true");await userEvent.click(screen.getByRole("button",{name:"Continue with my tutor"}));expect(api.savePreference).toHaveBeenCalledWith("arjun","ENGLISH")});it("renders focused voice feedback and fuller text-mode coaching without diagnostics",async()=>{render(<RouterProvider><ConversationScreen account={account} tutor={ananya} telugu/></RouterProvider>);await waitFor(()=>expect(api.conversation).toHaveBeenCalled());await userEvent.type(screen.getByLabelText("Your message"),"I go yesterday");await userEvent.click(screen.getByRole("button",{name:"Send"}));expect(await screen.findByLabelText("Current tutor response")).toHaveTextContent("Thanks for sharing");expect(screen.getByLabelText("Current grammar correction")).toHaveTextContent("went");expect(screen.getByText(/భూతకాలం/)).toBeVisible();expect(screen.queryByText(/Provider: openai/)).not.toBeInTheDocument();const avatar=screen.getByLabelText(/status: thinking/);expect(avatar).toHaveAttribute("data-mouth","REST");await userEvent.click(screen.getByRole("button",{name:"Text mode"}));expect(screen.getByText("confident")).toBeVisible()});it("provides text fallback when microphone is unavailable",()=>{Object.defineProperty(navigator,"mediaDevices",{configurable:true,value:undefined});render(<RouterProvider><ConversationScreen account={account} tutor={ananya} telugu={false}/></RouterProvider>);expect(screen.getByText("Microphone unavailable")).toBeInTheDocument();expect(screen.getByLabelText("Your message")).toBeEnabled()});it("produces no critical console errors",()=>{const spy=vi.spyOn(console,"error").mockImplementation(()=>undefined);render(<RouterProvider><DashboardScreen data={dashboard} tutor={arjun}/></RouterProvider>);expect(spy).not.toHaveBeenCalled()})});
