import{act,renderHook}from"@testing-library/react";import{beforeEach,describe,expect,it,vi}from"vitest";import{useMicrophone}from"../voice/useMicrophone";
class Recorder{static isTypeSupported=()=>true;state:RecordingState="inactive";ondataavailable:((e:BlobEvent)=>void)|null=null;onerror:(()=>void)|null=null;onstop:(()=>void)|null=null;constructor(_s:MediaStream,_o?:MediaRecorderOptions){}start(){this.state="recording"}stop(){this.state="inactive";this.onstop?.()}}const stop=vi.fn();const stream={getTracks:()=>[{stop}]}as unknown as MediaStream;
beforeEach(()=>{stop.mockClear();Object.defineProperty(window,"MediaRecorder",{configurable:true,value:Recorder});Object.defineProperty(navigator,"mediaDevices",{configurable:true,value:{getUserMedia:vi.fn().mockResolvedValue(stream)}})});describe("microphone consent boundary",()=>{it("never starts without consent",async()=>{const{result}=renderHook(()=>useMicrophone(false));await act(()=>result.current.start());expect(result.current.state).toBe("error");expect(navigator.mediaDevices.getUserMedia).not.toHaveBeenCalled()});it("requests permission only after start",async()=>{const{result}=renderHook(()=>useMicrophone(true));expect(navigator.mediaDevices.getUserMedia).not.toHaveBeenCalled();await act(()=>result.current.start());expect(result.current.state).toBe("recording")});it("handles denied permission",async()=>{vi.mocked(navigator.mediaDevices.getUserMedia).mockRejectedValue(new DOMException("denied","NotAllowedError"));const{result}=renderHook(()=>useMicrophone(true));await act(()=>result.current.start());expect(result.current.state).toBe("denied")});it("cancels and stops tracks",async()=>{const{result}=renderHook(()=>useMicrophone(true));await act(()=>result.current.start());act(()=>result.current.cancel());expect(result.current.state).toBe("cancelled");expect(stop).toHaveBeenCalled()});it("withdrawn consent blocks a new recording",async()=>{const{result,rerender}=renderHook(({consent})=>useMicrophone(consent),{initialProps:{consent:true}});rerender({consent:false});await act(()=>result.current.start());expect(result.current.state).toBe("error")})});
